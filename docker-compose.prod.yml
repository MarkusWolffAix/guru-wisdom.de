services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: prod
    ports:
      - "80:80"
    # WICHTIG: Keine Volumes!
    environment:
      - SERVER_NAME=:80
      - APP_ENV=prod
    restart: always
    tty: true